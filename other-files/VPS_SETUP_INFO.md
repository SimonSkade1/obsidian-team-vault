# Setting up the automation host

> **Not for members.** This file is only for whoever runs the *automation host* — the one machine that holds the vault's git repository and runs the nightly summary and cleanup jobs and the Discord bot (a small VPS, say — see the example below). If you just use the vault, `INSTALL_AND_SETUP.md` is your setup and you can ignore this file entirely. It is kept so the host can be rebuilt, moved to another machine, or handed to someone else.

**Exactly one machine does this.** The vault's git repository and its two scheduled jobs belong to that machine alone: run by several people they would collide (two repositories, two writers of the same summary note, two cleanups moving the same files). Everyone else follows `INSTALL_AND_SETUP.md` and never touches git here.

Pick the machine that is on most of the time, has a Claude Code subscription login, and already syncs this vault. The steps below are Linux; on macOS or Windows only the scheduling differs.

**Example — the original team's host (2026-09-19):** the Hetzner VPS `<HUB-NAME>` (<HOST-IP>), user `<user>`, vault at `/home/<user>/obsidian-team-vault`, Syncthing folder id `team-vault`, timer at 23:30 in the host's timezone — that machine runs UTC. Moving the host elsewhere = move the folder *including* `.git` (rsync), redo steps 3, 8, 9 and 21 there, remove the units on the old host, and re-share the Syncthing folder from the new one.

## Preflight

1. Work from the vault root — `README.md`, `me.base` and `.claude/` are in it.
2. Check what the jobs need, and install what is missing: `git --version`; `flock` and `uuidgen` (util-linux — `flock` may be absent on macOS, which then needs a small edit to both wrapper scripts); `python3 --version` (3.8+); `claude --version` plus a logged-in subscription account (step 13 proves the login works).
3. This machine already holds files the others must never receive, above all `.git`. So make sure `.stignore` exists in the vault root, containing the single line `#include .stignore-shared`, *before* you add the folder to Syncthing here — `.stignore-shared` is already on disk, so the include resolves right away. (Syncthing's **Edit → Ignore Patterns** tab writes the same file and works here too, including while you add the folder; writing it first is just easier to check — `cat .stignore`.)
	1. Then add the vault as a Syncthing folder **on this machine first** (any id the members' Syncthing does not use yet — currently `team-vault`; the label is free). Copy the versioning of your other folders if you want deleted files recoverable (currently: staggered, 1 year). Members are added afterwards, one command each: sub-item 4 below.
	2. **Hub and spoke, not a mesh:** this host is the only device that shares the folder with anyone. Every member accepts the share from *this* device and adds no other member's device to it. That keeps one place where the file set is authoritative, keeps a new member's onboarding to one accept, and stops a laptop that is offline for weeks from resurrecting deleted files for everyone. It costs the direct laptop-to-laptop path: while the host is down, members do not exchange edits.
	3. Wait until this folder reports **Up to Date** and 0 errors before telling a member to accept — they pull from here.
	4. **Adding a member** — the host half of `INSTALL_AND_SETUP.md` steps 2 and 5. They send you their device ID and first name; you run `other-files/add-member.sh <DEVICE-ID> <name>` on this machine. It adds the device and shares the folder through Syncthing's own REST API on localhost, so it needs no GUI and no ssh tunnel into this host, and re-running it changes nothing. With no arguments it prints the devices the folder is shared with and this host's device ID — the one the member sees in their notification. By hand it is the same two actions: **Add Remote Device**, then the folder's **Edit → Sharing**. (Verified against Syncthing 1.27.2 and 2.1.5; it reads the API key from `config.xml`, override the location with `STHOME` and the folder id with `VAULT_FOLDER_ID`.)
4. Create `_local/me.md` with your own `user:` line (three lines: `---`, `user: yourname`, `---` — the member route, `setup-member`, refuses to run on this host because of the `.git` here) — the summaries are written by a Claude session that reads the same bases everyone else sees.

## Git — the pipeline's backbone

The nightly job commits one snapshot per day and then summarizes that diff; the cleanup uses the history to scope its run. Both refuse to start without a repository. This repository is the automation's: it has no remote, it is never synced (`.stignore-shared` ignores `/.git`), and nobody clones it.

5. `git rev-parse --git-dir` — if it fails: `git init -b main`.
6. Make sure a commit identity exists (`git config user.name`, `git config user.email`; set them repo-locally if they are empty). The address ends up in every commit of this local repository.
7. If the repository has no commits yet: `git add -A && git commit -m "Initial vault snapshot"`.

## Scheduling

Two wrapper scripts in `other-files/` are the whole interface: `run-daily-review.sh` (nightly — commits the snapshot, writes the daily note in `periodic-auto-summaries/`, then any due weekly/monthly/quarterly/yearly note, and chains the cleanup) and `run-vault-cleanup.sh` (called by the first one, runs at most every second day). The nightly wrapper also runs `other-files/check-unassigned.py` (pure python, no Claude): when new unassigned items appear — neither `owner` nor `next_action_by`, status not terminal, the `Unassigned` view of `me.base` — it files an `inbox` task for the member set as `OWNER` in that script, in `projects-tasks-notes/`, or appends them to the open check task if there is one; an item is listed once, ever (every `!check unassigned items …` file, archived ones too, counts as coverage). Scheduling means invoking `run-daily-review.sh` once a night; 23:30 keeps it inside the quiet hour the cleanup wants, when other members are unlikely to have files open.

8. **Linux (systemd user units, shipped):** the units in `other-files/systemd/` are named `team-vault-daily-review.service` / `.timer` so they cannot collide with the units of another vault on the same account, and the service's `ExecStart` points at `%h/obsidian-team-vault/other-files/run-daily-review.sh` (`%h` = the running user’s home directory). **If the vault sits anywhere else, edit that line first** (a header comment marks it); edit the copy in the vault — `install.sh` links the units from here, so the vault copies stay the source of truth. Then `bash other-files/systemd/install.sh`, which links, enables and starts the timer and prints the next fire times.
	1. **The model** the two jobs run on is the `"model"` key of `other-files/scheduled-settings.json` and `other-files/cleanup-settings.json` (currently `opus`) — the wrappers pass those files with `--settings` and no `--model`. Change the model there, not in the scripts. Both files also deny `Read(~/.claude/**)`, `Read(~/.claude.json)` and `Read(~/.ssh/**)` (added 2026-09-23): Claude Code applies `Read` deny rules to plain shell reads too (`cat`, `head` of such a path are denied — tested on this host), so an injected nightly run cannot read `~/.claude/.credentials.json` that way; the allowed `Bash(python3:*)` still can — narrowing it to the scripts the jobs need is open.
	2. That key is also the fix for a headless run that dies with "There's an issue with the selected model": a `"model"` in the host user's `~/.claude/settings.json` applies to `claude -p` as well, and an alias that only interactive sessions accept then kills every scheduled run (it silently broke another vault for two nights in 2026-09). The `--settings` key wins over it, so setting it here makes the jobs independent of the host's interactive default. Check once: `claude -p "Reply with exactly: ok" --settings other-files/scheduled-settings.json --strict-mcp-config`.
9. **Then allow the timer to run without you: `sudo loginctl enable-linger $USER`.** A `systemctl --user` timer lives inside your login session — on a machine you only ssh into, the whole user manager is torn down when you log out and the job silently never fires again. Run this once on any host where you are not permanently logged in (a VPS above all); `install.sh` does not do it, because it needs root, but it warns you when lingering is off. On a desktop that stays logged in it is optional — `Persistent=true` in the timer catches up a run the machine slept through.
10. **macOS / Windows:** no systemd — set up the platform equivalent (a launchd plist, a Task Scheduler job) that runs `other-files/run-daily-review.sh` nightly with the vault as the working directory. A user must be logged in for the Claude session to run.

## Verify without spending tokens

11. `DR_COMPUTE_ONLY=1 bash other-files/run-daily-review.sh` — prints the review periods it would write, then exits.
12. `VC_GATE_ONLY=1 bash other-files/run-vault-cleanup.sh` — prints the cleanup's cadence decision (on a fresh vault: `LAST_RUN=none`).
13. `bash .claude/scripts/check-claude-usage.sh` — prints the plan's 5-hour and 7-day usage, and is the one check that proves the subscription login works in this shell. Nothing gates on it automatically: the wrappers do not call it (the VNA controller does), so if this host shares a plan with someone's interactive work, look here before adding more scheduled jobs.
14. `python3 .claude/scripts/read_obsidian.py "README.md"` — the read script runs.
15. `loginctl show-user $USER --property=Linger` — must print `Linger=yes` on any host you log out of; otherwise re-read step 9.
16. `systemctl --user list-timers team-vault-daily-review.timer` — shows when the job fires next.
17. If any command warns "this workspace has not been trusted", Claude Code ignores this vault's own `.claude/settings.json` allowlist. Run `claude` once interactively in the vault root and accept the dialog, or — on a host you only reach over ssh — set `projects["<vault path>"].hasTrustDialogAccepted: true` in `~/.claude.json` (done on the current host 2026-09-19; the warning line names the exact key).

## What the jobs will do

18. Each run commits whatever changed in the vault since the last snapshot — everyone's edits, under this machine's commit identity. There is no authorship inside the vault, so the summaries say what changed, not who changed it.
19. The cleanup moves files: it rebuilds the `projects-tasks-notes/` folder tree from the `parent` properties and moves finished trees into `archived/`. Under Syncthing a move is a delete plus a create, and it cannot see which notes are open on other people's machines — it skips files modified in the last 30 minutes, which is why it runs in the middle of the night. Its documentation is `other-files/vault-cleanup routine.md`, the open questions it collects `other-files/vault-cleanup log.md`.
20. Told to stop: disable the timer (`systemctl --user disable --now team-vault-daily-review.timer`). Nothing else in the vault depends on these jobs.

## Discord bot

The team's Discord bot (`@Claude` in the infra server `PauseAI_Global_Infrastructure_Team`, and DMs) is one long-lived headless Claude Code session on this host: the user unit `pai-discord-bot` runs `other-files/discord/run-bot.sh`, which starts `claude -p` in the vault with `--channels plugin:discord@claude-plugins-official`, the plugin enabled for that session alone (its header comment explains the mechanics). Vault files, synced, no secrets: `run-bot.sh`, `bot-settings.json` (model, tool allowlist, the Stop hook; `dontAsk` = anything not allowed is refused, nothing ever prompts), `bot-prompt.md` (persona, rules, the member table Discord user id → vault name), `reply-guard.py` (the Stop hook, item 24), `other-files/systemd/pai-discord-bot.service`. Host state outside the vault, all under `~/.claude/channels/discord/`: `.env` (the token), `access.json` (who may trigger the bot), `session/` (the launcher's files). The other direction — a laptop session reading or posting to Discord — is `other-files/discord/discord.py`, which needs nothing but the token.

21. **Rebuild** (from a shell on the host or over ssh; nothing needs root):
	1. Bun: `curl -fsSL https://bun.sh/install | bash`. It needs `unzip`; the current host has none and no passwordless sudo, so the zip was unpacked by hand: `curl -fsSLo /tmp/bun.zip https://github.com/oven-sh/bun/releases/latest/download/bun-linux-x64.zip && python3 -m zipfile -e /tmp/bun.zip /tmp && mkdir -p ~/.bun/bin && mv /tmp/bun-linux-x64/bun ~/.bun/bin/ && chmod +x ~/.bun/bin/bun` (`bun-linux-x64-baseline.zip` on a CPU without AVX2). Check: `~/.bun/bin/bun --version` (1.4.2 on 2026-09-22).
	2. Plugin: `claude plugin install discord@claude-plugins-official` (user scope; lands with its dependencies in `~/.claude/plugins/cache/claude-plugins-official/discord/<version>/`), then `claude plugin disable discord@claude-plugins-official` — `~/.claude/settings.json` must show `"enabledPlugins": {"discord@claude-plugins-official": false}`. Disabled host-wide, no other session on this host (the nightly jobs, anything interactive) connects to Discord; the bot session alone turns it on through `enabledPlugins` in `bot-settings.json`. Then the vault's patch, inside the version directory the plugin runs from (the newest under `…/discord/`): `cp -n server.ts server.ts.orig && patch -N server.ts ~/obsidian-team-vault/other-files/discord/server-wildcard.patch` (group key `*` = every channel, 21.4; and `messageCreate` drops Discord system messages — a thread started from one of the bot's posts emits a thread-starter message that references that post, which otherwise reached the session as an empty turn, seen 2026-09-23; attachments go to `$DISCORD_INBOX_DIR`, which `run-bot.sh` sets to `~/discord-inbox` because the plugin's default `~/.claude/channels/discord/inbox` is unreadable for the session, and get their file extension from the content type — Discord names a webp body `image.png`; and audio attachments, i.e. voice messages, are transcribed through OpenAI before delivery, 2026-09-24: `OPENAI_API_KEY` from `.env` (21.3), model `gpt-4o-mini-transcribe` unless `.env` sets `OPENAI_TRANSCRIBE_MODEL=`, the transcript becomes the message text and the `<channel>` tag gets a `transcript` attribute naming the file; without a key or on failure the message arrives as before, `(attachment)` plus the attachment list, and the reason is in the plugin server's stderr, `~/.cache/claude-cli-nodejs/-home-simon-PauseAI-Global-shared-vault/mcp-logs-plugin-discord-discord/`; and quick capture, 2026-09-24: a member's text-only message starting with `!`, `,` or `.` (not `..`) is written by the server itself into `projects-tasks-notes/` — owner from the member table in `bot-prompt.md`, re-read per message, the vault from `DISCORD_VAULT_DIR`, set by `run-bot.sh` — and answered `added file to inbox: …`; the session never sees it); a plugin update is a new version directory, so re-apply it there and restart.
	3. Token: `mkdir -p -m 700 ~/.claude/channels/discord` here, then from Simon's laptop `scp ~/.claude/channels/discord/bot.env simon@<HOST-IP>:.claude/channels/discord/.env`, then `chmod 600 ~/.claude/channels/discord/.env` — two lines, `DISCORD_BOT_TOKEN=…` and `OPENAI_API_KEY=…` (Simon's OpenAI key, since 2026-09-24; read by the plugin server alone, for voice-message transcription — the session cannot `Read` `~/.claude/**`, also not through the shell, its `Bash` is limited to two `discord.py` subcommands, and `reply` refuses to attach files from that directory, so it never sees either secret; the remaining exposure is the nightly jobs' `Bash(python3:*)`, 8.1, and anyone with a shell as this user). Both live in these two files and nowhere else: never in the vault, never in a log.
	4. `~/.claude/channels/discord/access.json` (mode 600; schema and every key in the plugin's `ACCESS.md` next to `server.ts`). Content on 2026-09-23 (the server adds a `pending` key of its own):
	   ```json
	   {
	     "dmPolicy": "allowlist",
	     "allowFrom": ["344481366408298496", "925051691798233089", "252771041464680449", "178873242914783232", "285884980641660929"],
	     "groups": { "*": { "requireMention": true, "allowFrom": [] } },
	     "ackReaction": "👀",
	     "replyToMode": "first",
	     "chunkMode": "newline"
	   }
	   ```
	   `allowFrom` = the Discord user ids that may DM the bot and trigger it in channels (simon, matilda, maxime, harry, jakub on 2026-09-23; keep it equal to the member table in `bot-prompt.md`); `groups["*"]` (added by the vault's patch, 21.2) = every channel of every server the bot is in, on an @mention or a reply to the bot, senders from `allowFrom` only (threads count as their channel); `dmPolicy: allowlist` drops DMs from anyone else silently. The plugin's own semantics still work next to it: a channel-id entry `{"requireMention": true, "allowFrom": []}` lets any member trigger the bot in that channel.
	5. Unit: `bash other-files/systemd/install.sh` links, enables and starts `pai-discord-bot.service` from the vault (next to the review timer; the unit's two paths assume the vault at `~/obsidian-team-vault`, see its header comment). Lingering (step 9) must be on. Check: `other-files/discord/run-bot.sh log --since -5min` shows a session line with `mcp: plugin:discord:discord=connected` and the start turn answered `ok`; then `other-files/discord/run-bot.sh send "post the word online to #general"` and `python3 other-files/discord/discord.py read '#general' -n 3`; the inbound direction only a human can test: an @mention by someone in `allowFrom` must get a reply within a minute (👀 alone means the server took it but the session did not).
22. **Operations:**
	1. Status and logs: `systemctl --user status pai-discord-bot`; `other-files/discord/run-bot.sh log [-f] [--since -30min]` prints the journal readably (session starts with model, mode and MCP status; operator turns; assistant text; tool calls; cost and denials per turn); raw: `journalctl --user -u pai-discord-bot -o cat`. Nothing else is logged; on Discord, the 👀 reaction marks every message the bot received.
	2. Restart: `systemctl --user restart pai-discord-bot` — a new session is connected within about a minute; messages sent meanwhile are lost (no catch-up). The session also ends itself at 04:00 UTC every day and is restarted; that, or a restart, is when edits to `bot-prompt.md`, `bot-settings.json` and `run-bot.sh` take effect. After editing the unit file: `bash other-files/systemd/install.sh && systemctl --user daemon-reload && systemctl --user restart pai-discord-bot`. `access.json` is re-read on every message: no restart.
	3. Talk to the session as operator: `other-files/discord/run-bot.sh send "text"` — the answer appears in the journal, not on Discord unless you ask for a post.
	4. Add a member: their Discord user id (Discord → User Settings → Advanced → Developer Mode on; right-click the person → Copy User ID) as a row of the table in `bot-prompt.md` (items get created for them from the next start on) and into `allowFrom` of `access.json` (DMs and channel mentions; effective at once). Both by hand: the plugin's `/discord:access` skill is not loaded in the bot's session.
	5. Channels: all of them, through `groups["*"]` (21.4); a channel the bot cannot see needs a Discord permission overwrite for its role (22.9). To limit the bot to some channels, replace `"*"` by channel-id entries (ids: `python3 other-files/discord/discord.py channels`) and say so in `README.md`, "Claude on Discord".
	6. Rotate the token: Developer Portal → Applications → the bot → **Bot** → **Reset Token** (the old token dies at once), the new line into the laptop's `bot.env` and this host's `.env` (21.3), then `systemctl --user restart pai-discord-bot` (the server reads the token at start).
	7. Model and allowlist: `bot-settings.json` (`model: sonnet`; `WebSearch`/`WebFetch` allowed since 2026-09-23 at Simon's request, `discord.py channels`/`read` for Discord context (never `post`), `Read` of `~/discord-inbox/**` for attachments, everything else vault-only; deny beats allow; `dontAsk` refuses everything else, and `--permission-prompts none` means nothing is ever relayed to Discord). Cost on this host's subscription, the same account as the nightly jobs: about $0.01–0.13 per turn, plus one `ok` turn per start; voice messages additionally cost about $0.003 per minute of audio on Simon's OpenAI account (measured 2026-09-24: a 10-second message transcribed in 1.7 s).
	8. Stop: `systemctl --user disable --now pai-discord-bot`. Never run a second session with the same token (Simon's laptop launcher `~/.claude/channels/discord/bot-session.sh` stays off): two connections answer every message twice.
	9. Discord side: the bot user is `Pausevaultbot` (renamed from `ClaudeTestBot` on 2026-09-23; the application itself is still named `ClaudeTestBot`; Discord rejects `Claude` as an app name, error 50277, also through the API); in the infra server the bot carries the nickname `Claude` (`PATCH /guilds/<id>/members/@me {"nick": "Claude"}`, 2026-09-23), so members write `@Claude`. Its managed role `Pausevaultbot` (1550252099307175969, renamed from `ClaudeTestBot` 2026-09-23), has exactly View Channels, Send Messages, Send Messages in Threads, Read Message History, Embed Links, Attach Files, Add Reactions (permissions `274878024768`), and the `Private` category with its three channels has an overwrite allowing that role View Channel — all the bot needs; the `Admin` role Simon gave it on 2026-09-23 is temporary (still on the bot when the project closed). **Public Bot** is off since 2026-09-23 (`GET /applications/@me` → `bot_public: false`; switching it off requires the Installation tab's Install Link set to None first).
23. **Channel route and the claude.ai connectors** (history of a deviation put to Simon at the checkpoint, reversed on 2026-09-23): on 2026-09-22 the launcher ran the plugin's `server.ts` itself as MCP server `discord` with `--mcp-config … --strict-mcp-config --channels server:discord`, because `--channels plugin:…` also loaded every claude.ai connector of this host's account (Gmail, Google Drive, Calendar, Slack, Claude Docs, … — eight, 85 tools, $0.13–0.16 per turn from their definitions alone). Found 2026-09-23: a `server:` entry never registers as a channel in a headless session (with or without `--dangerously-load-development-channels`; tested with a fake channel server) — the tools worked and messages got the 👀, but no inbound message ever became a turn. Fix: `~/.claude/settings.json` has `"disableClaudeAiConnectors": true` (verified: a plain `claude -p` session lists no MCP server; the nightly scripts additionally pass `--strict-mcp-config`), so the launcher uses the plugin route `--channels plugin:discord@claude-plugins-official` with the plugin enabled by `bot-settings.json`, no `--strict-mcp-config` (that would drop the plugin's server); the session's only MCP server is `plugin:discord:discord`, tools `mcp__plugin_discord_discord__reply|react|edit_message|fetch_messages|download_attachment`. Delete the settings key to get the connectors back in interactive sessions here. The plugin's `/discord:access` skill is loaded but unusable (`Skill` denied); `access.json` is edited by hand. Known and harmless: in `dontAsk` mode Claude Code still auto-approves read-only shell commands (`grep`, `ls`, `head`) on files inside the vault — the same data as `Read`.
24. **Reply guard**: on 2026-09-23 two answers (one in a thread, one DM) were written as plain text only — the journal had them, Discord never did; Sonnet skips the `reply` tool now and then when no other tool was needed. `bot-settings.json` therefore runs `other-files/discord/reply-guard.py` as a Stop hook after every turn: when the last human turn had `<channel … chat_id>` tags without a matching `reply` call, it blocks the stop once and names the chat_id(s) to answer (the second stop passes, so it cannot loop). `~/.claude/channels/discord/session/reply-guard.log` has one line per turn (`pass` / `block <chat_ids>` / `continue`); the `block` lines count how often it was needed. Verified 2026-09-23 with a haiku session: hooks in a `--settings` file work in `-p` mode.
25. **Outbox** (2026-09-23): members' machines have no token, so `discord.py post` there writes `{"channel","text","from","guild"}` as `<UTC stamp>-<user>-<hex>.json` into `other-files/discord/outbox/` (synced); on this host the unit `pai-discord-outbox` (`other-files/systemd/`, linked by `install.sh`) runs `discord.py deliver --loop 5`: it posts each file as the bot, appends the receipt to `other-files/discord/outbox.log` (`<time> <file> -> <#channel|id> msg <ids>` or `... FAILED: <why>`) and deletes the file — on failure too, so nothing piles up; only "Discord unreachable" keeps the files for the next round. The sender's command waits up to 45 s for its log line to sync back (measured 2026-09-23: posted after 11 s, receipt after 23 s). Trust: whoever can write into the vault can post as the bot; members are trusted. Check: `systemctl --user status pai-discord-outbox`, `journalctl --user -u pai-discord-outbox -o cat` (one line per delivery); after editing `discord.py`: `systemctl --user restart pai-discord-outbox` (the loop keeps the old code until then).
